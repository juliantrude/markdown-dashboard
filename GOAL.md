# GOAL

<!--
  Single source of truth for the /advance-goal loop (driven headless via the
  one-shot systemd chain, or interactively via `/loop /advance-goal`).
  The loop reads and rewrites this file every iteration.
-->

## North star

Build **md-dashboard**: a CLI-launched Node/TypeScript tool that turns a single
Markdown file into a **responsive, live-updating web dashboard**. Running
`md-dashboard <file.md>` starts a local server and opens a browser showing a
card grid — each `##` section becomes a card. Every Markdown element is rendered
to a matching widget; data-bearing elements (tables, task lists, numeric lists,
single metrics) can be **toggled at runtime between equivalent chart types** and
a faithful "Markdown" raw-render mode. Editing the file updates the dashboard
**live** (< ~1s) over WebSocket without a manual reload, preserving each widget's
chosen view. The element→widget mapping is authoritative in `ELEMENTS.md` (seeded
from the table agreed during /grill-goal). The tool is **read-only** toward the
user's content — it never writes back to the Markdown file.

Stack: Node/TypeScript, Vite, markdown-it (parsing), mermaid (diagrams), a chart
library (Chart.js or ECharts — decide in the scaffold increment), chokidar
(file watch), WebSocket (live reload), Playwright (E2E verification).

## Definition of Done

<!-- "fully blocked externally" does NOT count as done — this is a solo
     greenfield project with no expected external blockers. -->
- [x] `md-dashboard <file.md>` starts a local server and opens a responsive dashboard; every `##` section renders as a card in a responsive grid.
- [x] Every Markdown element in `ELEMENTS.md` renders to its default widget (prose, headings, blockquote, code, image, HR, table, task list, numeric/key-value list, single metric, ```mermaid, ```chart).
- [x] Data widgets (table, task list, numeric list, single metric) can be switched via an on-card toggle between the defined equivalent chart types AND a faithful "Markdown" raw-render mode (`- [ ]` becomes a real checkbox, etc.); task/progress widgets show each individual milestone's done/open state; the chosen view persists per widget in localStorage, survives live reload, and never mutates the file.
- [x] Editing the `.md` file updates the dashboard live (< ~1s) with no manual reload, and widget toggle state is preserved across the update.
- [x] The dashboard supports light and dark mode: the default follows the browser's `prefers-color-scheme`, a manual toggle overrides it, and the manual choice persists (localStorage) across reloads.
- [x] `npm run build`, `npm run typecheck` (tsc --noEmit), and `npm test` (Playwright E2E smoke) all pass green, and the repo is pushed to the public GitHub remote.

## Working Agreements (read every iteration — these override convenience)

**Priority order per iteration:**
1. If the user left a note/question in `## Status` (Blockers) or `## Log`, address that first.
2. Then take the next unchecked increment from `## Plan`.

**Git workflow:**
- Remote: public GitHub repo `github.com/juliantrude/markdown-dashboard` (SSH). Create it in Increment 1 via `gh repo create juliantrude/markdown-dashboard --public --source=. --remote=origin --push` (repo name is the corrected spelling — the local folder `markdown-dasboard` has a typo; keep the folder as-is).
- Per increment: one clean commit **directly on `main`**, then `git push`. No feature branches, no PRs.
- Commit messages in English, imperative mood, stating what + why. End every commit with the trailer `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- **Verification bar before any commit:** `npm run build` green, `tsc --noEmit` green, AND the Playwright E2E smoke green. Never commit on a red verification run — fix or revert first.
- No force-push, no history rewrites on `main`. Never commit secrets or tokens.
- **Scaffolding safety (Node is v18 here):** never run interactive scaffolders (`npm create vite` in prompt mode selects destructive "Remove existing files" when fed piped input — it once deleted this GOAL.md/CLAUDE.md). Use only non-interactive, Node-18-pinned commands (e.g. `npm create vite@5 <dir> -- --template vanilla-ts`, or hand-write the config). Make the very first commit as early as possible so untracked files are never at risk.

**Out-of-scope systems (never modify):**
- The user's Markdown input files — the tool is strictly read-only toward content; a bug that writes back to the source file is a release blocker.
- Anything outside `/home/julian/projects/markdown-dasboard`.
- The imported team-core `GUIDANCE.md` (Poker Analysis Platform: GitLab repos, `uv`, Vue, MR etiquette) **does not apply here** — this is a standalone Node/TS project. Do not follow its GitLab/MR/uv/service-architecture rules. Escalation for anything ambiguous: leave a note in `## Status` Blockers and pick the next unblocked increment.

**Etiquette:**
- Code comments, commit messages, README, and in-app UI copy in English (public repo convention).
- Clean, idiomatic TypeScript; match established patterns as the codebase grows; keep functions small and typed.

**References:**
- Docs to consult/copy idioms from: markdown-it, mermaid, the chosen chart lib (Chart.js/ECharts), chokidar, Vite, Playwright.
- `ELEMENTS.md` is the authoritative element→widget mapping; update it (and note why) if a mapping changes.
- Constraint: Node is v18 on this machine — pin a Vite/toolchain version that runs on Node 18, or document a required Node upgrade in the Log rather than silently breaking the build.

## Plan

<!-- One increment per iteration; check off only when done AND verified. -->

### Scaffold & repo
- [x] Increment 1 — git init; create the public GitHub repo `markdown-dashboard` (gh) with SSH remote; add `.gitignore` + README stub; scaffold a Vite + TypeScript skeleton with a runnable dev server showing an empty "Dashboard" shell; `npm run build` + `tsc --noEmit` green; initial commit + push.
- [x] Increment 2 — set up Playwright + one smoke test (server starts, page loads, shell title visible); wire `npm test`; commit `ELEMENTS.md` (the agreed mapping table) and `ARCHITECTURE.md` (module overview, live-reload flow). Full verification pipeline green.

### CLI, server & live reload (core infra)
- [x] Increment 3 — CLI entrypoint `md-dashboard <file.md>`: start a local HTTP server, read the file, serve the dashboard shell, open the browser. Smoke: run CLI against a sample `.md`, server responds with the shell.
- [x] Increment 4 — chokidar file watch + WebSocket channel: on file change push to the client and re-render. Smoke: change the sample file → dashboard updates in < ~1s with no manual reload.

### Markdown parsing → card grid
- [x] Increment 5 — markdown-it parsing + section splitter: `##` boundaries produce cards in a responsive grid; render prose, headings, blockquote, code block, image, horizontal rule. Smoke: sample file → expected card count + prose visible.

### Widgets & toggles
- [x] Increment 6 — widget framework: per-card toggle (icon buttons, top-right) including the faithful "Markdown" raw-render mode (real checkboxes for `- [ ]`, proper lists); selection persisted in localStorage, survives live reload, never mutates the file. Smoke: toggle switches the view and the choice sticks after reload.
- [x] Increment 7 — table widget: table ↔ Bar / Grouped Bar / Stacked Bar / Line / Area / Pie / Donut / Radar / Scatter (offer only the types valid for the data shape); integrate the chart lib. Smoke: a table renders as Bar and switches to Pie.
- [x] Increment 8 — task-list widget: checklist ↔ progress bar / donut, with each milestone's done/open state clearly visible. Smoke: mixed `- [ ]`/`- [x]` → correct percentage AND per-item status shown.
- [x] Increment 9 — numeric/key-value list → stat tiles (KPI) ↔ Bar/Pie; a single `Metric: value` → stat tile ↔ gauge. Smoke: both render and toggle.
- [x] Increment 10 — ```mermaid fences → Mermaid diagram; ```chart fences (JSON/YAML config) → explicit chart with its valid alternatives. Smoke: both render.

### Polish & responsiveness
- [x] Increment 11 — responsive breakpoints (mobile/tablet/desktop); light/dark theming: default from `prefers-color-scheme`, a manual light/dark toggle that overrides it, choice persisted in localStorage; robust toggle-state persistence; error handling (invalid/empty markdown). Playwright smokes across viewports and both color schemes.
- [x] Increment 12 — complete README/docs (usage, examples, a demo GIF or screenshots) and ship a sample dashboard `.md` as a demo fixture.

### Folder support (later, as agreed)
- [x] Increment 13 — accept a folder as input: watch all `.md` files, add sidebar navigation between dashboards, each file its own dashboard. Smoke: two files → both reachable, each renders.

### Closeout
- [x] Final sweep: everything at spec or documented-blocked; DoD checkboxes above updated.

## Status

STATUS: COMPLETE
**Next action:** none — Definition of Done met, no increments remain.
**Blockers:** none.

## Budget

- session pause threshold: 85%
- weekly pause threshold: 90%

## Log

<!-- Append-only, dated. One line per iteration. Newest at the bottom. -->
- 2026-07-17 — GOAL.md created via /grill-goal; nothing started yet
- 2026-07-17 — Increment 1 attempt: `npm create vite@latest` failed (requires Node 20+, this machine has Node 18.19.1). Retried with `npm create vite@5` pinned for Node 18 compat; the non-interactive prompt was mishandled and selected "Remove existing files and continue", which deleted GOAL.md, CLAUDE.md, and .hustle/ (all untracked, not yet committed). Recovered: GOAL.md and CLAUDE.md restored verbatim from conversation context; .hustle/ restored by rerunning hustle-setup.sh (idempotent scaffolding, systemd unit had survived independently). No project code existed yet at time of loss, so no other work was lost. Root cause: piping `y` into an arrow-key-driven interactive prompt instead of using a non-interactive flag — must avoid `npm create vite` interactive mode entirely from now on and use `--yes`/explicit non-interactive scaffolding instead.
- 2026-07-17 — Increment 1 completed: found the Vite vanilla-ts scaffold already present untracked from the prior attempt (index.html, src/, tsconfig.json, package.json all valid). Retitled shell to "md-dashboard"/"Dashboard", stripped the template's counter/logo boilerplate (removed src/counter.ts, src/typescript.svg, public/vite.svg, the favicon link) down to an empty `<h1>Dashboard</h1>` shell, renamed package.json to `markdown-dashboard`, added a `typecheck` script, wrote a README stub. Ran `npm install` (12 packages), `npm run build` and `npm run typecheck` both green, and confirmed via `curl` against the running dev server that it serves the shell with the correct title. Created the public GitHub repo `juliantrude/markdown-dashboard` via `gh repo create`; first push over SSH failed with "Host key verification failed" (non-interactive shell, no ssh-askpass) — fixed by `ssh-keyscan github.com >> ~/.ssh/known_hosts` and retrying with `GIT_SSH_COMMAND="ssh -o BatchMode=yes"`. Root commit `a54b787` pushed clean to `main`. Noted for next iteration: `ELEMENTS.md` was never actually written to disk despite being referenced as already-seeded in the North Star — Increment 2 needs to author it (from the widget/chart-type notes already in `## Plan`), not just commit an existing file.
- 2026-07-17 — Blocker cleared by the user (not the loop): `ELEMENTS.md` authored directly on disk with the authoritative element→widget mapping table confirmed verbatim during /grill-goal (better source than reconstructing from Plan notes). Increment 2 should just commit it alongside ARCHITECTURE.md — do not rewrite it.
- 2026-07-17 — Increment 2 completed: installed `@playwright/test` (^1.61.1, runs fine on Node 18.19.1) and its Chromium browser; added `playwright.config.ts` (webServer boots `npm run dev` on :5173, self-contained) and `tests/smoke.spec.ts` (title + "Dashboard" heading visible); wired `npm test` → `playwright test`. Wrote `ARCHITECTURE.md` documenting the planned module layout (cli, server, watch, parser, widgets) and the live-reload flow (chokidar → diff → WebSocket push → client re-render preserving localStorage toggle state) ahead of those modules actually existing, per the GOAL.md plan. Committed `ELEMENTS.md` (authored by the user in the prior iteration) unchanged, alongside `ARCHITECTURE.md`, the Playwright config/test/scripts, and `.gitignore` additions for Playwright artifacts. Full verification green: `npm run build`, `npm run typecheck`, `npm test` (1 passed).
- 2026-07-17 — Increment 3 completed: added `src/cli.ts` (argv parsing for `<file.md>`/`--port`/`--no-open`, file-exists/`.md`-extension validation, exit code 1 with usage on error) and `src/server/server.ts` (Node `http` server serving `dist/` — the Vite-built shell — with a path-traversal guard and SPA fallback to `index.html`; confirms the target file is readable but doesn't parse it yet, per plan) and `src/server/open-browser.ts` (best-effort `open`/`xdg-open`/`start` cross-platform launch that only warns on failure, so it's safe in headless/CI). Split the TypeScript build in two so Node and DOM ambient types never mix: root `tsconfig.json` now excludes `src/cli.ts`/`src/server` and sets `"types": []`; new `tsconfig.server.json` (Node lib+types, `NodeNext` resolution, `outDir: dist-server`) compiles the CLI/server. `bin/md-dashboard.js` is a tiny shebang wrapper importing the compiled `dist-server/cli.js`; `package.json` `bin` field points at it. `npm run build`/`typecheck` now run both tsconfigs; added `@types/node` (^18.19, matches the Node 18.19.1 runtime). Added `tests/fixtures/sample.md` and `tests/cli.spec.ts` (spawns the built CLI, waits for its stdout ready-line, drives a real Playwright page against it, kills it in `afterAll`) — a second, independent smoke path from `smoke.spec.ts`'s dev-server test, and the first test that actually exercises `npm run build`'s output. Manually verified CLI error paths too (missing arg, missing file, wrong extension) — all exit 1 with a clear message. Full verification green from a clean `dist`/`dist-server`: build, typecheck, `npm test` (2 passed). Committed `12077ec`, pushed to `main`.
- 2026-07-17 — Increment 4 completed: added `src/server/watch.ts` wrapping `chokidar` — pinned to `^4` (v5 requires Node ≥20.19; this machine runs 18.19.1) — with `awaitWriteFinish` and listening on both `change` and `add` (atomic-save editors emit `unlink`+`add`), invoking a callback with the fresh file content. `src/server/server.ts` now runs a `ws` `WebSocketServer` on the same HTTP server's `/ws` path: sends current file content on connect, broadcasts fresh content to all open sockets on every watch callback; `close()` now tears down the watcher and `wss` too. `src/main.ts` opens the `/ws` connection on load, writes incoming `{type:'content', content}` messages into a new `#content` `<pre>` element, and auto-reconnects on drop. The content payload is intentionally raw file text, not parsed cards — Increment 5's parser will swap the payload shape without touching this connection/broadcast plumbing (noted in `ARCHITECTURE.md`, which was updated throughout to mark the watch/WS pieces "present" instead of "planned"). Added `tests/watch.spec.ts`: spins up the built CLI against its own temp-directory fixture (own port 4320, isolated from `cli.spec.ts`'s `tests/fixtures/sample.md`), asserts initial content renders, edits the file mid-test, and asserts the DOM updates within 2s — observed ~300ms in practice, well under the <1s target. Full verification green from a clean `dist`/`dist-server`: build, typecheck, `npm test` (3 passed, all green). Committed `9760734`, pushed to `main`.
- 2026-07-17 — Increment 5 completed: added `markdown-it@^14` + `@types/markdown-it@^14` and `src/parser/parse.ts` (`parseDocument`), which walks markdown-it's token stream once, takes the first `#` heading as `title`, and splits on `##` `heading_open` tokens into `Card[]` (`{ heading, html }`, body rendered via `md.renderer.render` on that section's tokens). Content before the first `##` boundary (other than the title) is deliberately dropped — every card lives under a `##`, per `ELEMENTS.md`; this is a design decision worth knowing about if a future increment wants a "preamble" card. `src/parser/` compiles under `tsconfig.server.json` (Node-side only, added to its `include`; excluded from the browser `tsconfig.json`) since parsing happens server-side, not in the browser bundle. `src/server/server.ts` now calls `parseDocument` on initial connect and on every watch callback, sending `{ type: 'content', title, cards }` over `/ws` instead of raw text. `src/main.ts` renders a `#doc-title` subtitle plus one `.card` (`.card-heading` + `.card-body`) per section into `#content` (now a grid `<div>`, not a `<pre>`); only the heading text is escaped client-side since markdown-it (`html: false`) already escapes the body HTML server-side. Added `.dashboard-grid`/`.card` responsive grid CSS (`auto-fill`/`minmax`, no fixed breakpoints — that's Increment 11) with light/dark variants. Added `tests/fixtures/elements.md` (prose, bold/italic, an h3 sub-heading, blockquote, code block, image, hr, and a paragraph before the first `##` to prove it's dropped) and `tests/parser.spec.ts` (card count, title extraction, dropped-preamble check, and that every Increment-5 element type renders); had to add `test.describe.configure({ mode: 'serial' })` to that file because `playwright.config.ts`'s `fullyParallel: true` was scheduling its two tests onto separate workers, each spawning its own CLI on the same port (`EADDRINUSE`, silently manifesting as a `beforeAll` timeout) — `cli.spec.ts`/`watch.spec.ts` never hit this since they're one test each. Updated `tests/watch.spec.ts`'s fixture to add a `## Status` heading (its old preamble-only content would now be silently dropped by the parser). Updated `ARCHITECTURE.md` throughout: parser module marked present, WS payload shape, live-reload flow, and the verification/test-suite section. Full verification green from a clean `dist`/`dist-server`/`test-results`: build, typecheck, `npm test` (5 passed).
- 2026-07-17 22:32 — Budget re-check before starting Increment 7: session still at 92% (previous check at 22:17 saw 91%, hadn't reset yet — reset is 2026-07-18 01:50am Europe/Berlin). No work done this run to avoid starting a chart-lib integration mid-session-limit; re-armed for 2026-07-18 01:52 (session reset + 2min buffer) instead of another short poll, since the session won't reset for ~3h and polling every ~15min would just burn week budget for no progress. Week budget is healthy (34%). Next run should proceed straight to Increment 7.
- 2026-07-17 — Increment 6 completed: `Card` now carries two renders — `html` (unchanged default widget render) and `markdownHtml` (the faithful "Markdown" raw-render mode from `ELEMENTS.md`). Added `markCheckboxes()` in `src/parser/parse.ts`: a small hand-written token-mutation pass (no new dependency) that finds `inline` tokens whose first child is text matching `/^\[([ xX])\]\s+/`, strips the bracket marker, and unshifts a manually-constructed `html_inline` `Token` containing a real `<input type="checkbox" disabled>` (checked if `[x]`/`[X]`) — confirmed `html_inline` tokens render their raw `content` regardless of the `html:false` parser option (that option only gates *parsing* literal HTML in the source, not rendering tokens inserted programmatically). `flush()` renders `html` from the untouched tokens *before* calling `markCheckboxes()`, then renders `markdownHtml` from the same (now-mutated) array — order matters since both renders share one token array. `src/main.ts` adds a small generic `WidgetView[]` framework per card (currently two views: `default` → `card.html`, `markdown` → `card.markdownHtml`); Increments 7-10 extend this by contributing more view ids (chart types, progress, KPI) rather than restructuring it. Icon toggle buttons render top-right of each card (`.card-header`); selection is looked up/written to `localStorage` under `md-dashboard:view:<heading>` (heading assumed unique within a doc, not enforced) so it survives both a live-reload push and a full page reload. One delegated `click` listener on `#content` handles all toggle buttons, so it keeps working across the full-grid rebuilds that live reload already does — no per-render re-binding needed. Added `.card-header`/`.card-toggle`/`.toggle-btn` CSS (light+dark variants) and a small `.card-body input[checkbox]` margin rule. Added `tests/fixtures/widgets.md` (a two-item task list, one done one open) and `tests/widgets.spec.ts` (default view has zero checkboxes and shows literal `[ ] Buy milk` text — the checklist *widget* itself is Increment 8, this increment only adds the toggle mechanism and the raw-render alternative; clicking the Markdown toggle shows two real checkboxes with correct checked state; a second test confirms the choice survives `page.reload()`); needed the same `test.describe.configure({ mode: 'serial' })` fix as `parser.spec.ts` for the same reason (two tests sharing one spawned CLI on one port, `fullyParallel` was racing a second spawn onto the same port). Updated `ARCHITECTURE.md`'s parser/widgets/main.ts sections and verification list. Full verification green from a clean `dist`/`dist-server`/`test-results`: build, typecheck, `npm test` (7 passed). Note for Increment 7: the North Star says "decide [Chart.js vs ECharts] in the scaffold increment" but no such decision was ever logged in Increments 1-2 — Increment 7 needs to make and record that call before integrating a chart lib.
- 2026-07-18 — Increment 7 completed. Chart lib decision (never logged in Increments 1-2 as intended): **Chart.js** — built-in bar/line/pie/doughnut/radar/scatter cover every `ELEMENTS.md` alternative without a plugin, no Node-version constraints (pure browser lib), and its imperative `new Chart()`/`chart.destroy()` API maps directly onto this project's per-card mount/toggle/rebuild lifecycle; ECharts passed over as heavier/more config-first than needed. Read the dataviz skill before writing any chart code per its trigger; used its validated 8-slot categorical palette verbatim (fixed hue order, `foldToOther` past 8 series/categories, legend only for 2+ series or always for pie/donut, thin/rounded bar marks, 2px lines, ~10% area fill) rather than picking colors by eye — ran `scripts/validate_palette.js` for both light and dark mode, both PASS (light has an expected WARN on 3 low-contrast hues, mitigated per the skill by the always-available table view). Added `src/parser/table.ts` (server-side, new file): `extractTableData` walks a card's token stream for its first `table_open`…`table_close`, takes column 0 as `categories`, and keeps every other column as a numeric `series` only if *every* row parses as a number; `validChartTypes` then decides which of the 9 chart types fit the shape (rules invented this increment, documented in `ARCHITECTURE.md` and inline: `bar` needs exactly 1 series, `bar-grouped`/`bar-stacked`/`scatter` need 2+ series, `line`/`area` need 2+ categories, `pie`/`donut` need 1 series + 2+ categories, `radar` needs 3+ categories). `parse.ts`'s `flush()` calls this before `markCheckboxes()` mutates the tokens (table extraction doesn't care about checkbox mutation, but ordering was already load-bearing for the two HTML renders, so it's set up first for clarity) and adds `table`/`chartTypes` to `Card`. Added `src/widgets/chart-view.ts` (client-only, new module, `chart.js@^4.5.1` installed — no engine constraints, safe on Node 18): builds one `ChartConfiguration` per type (bar/grouped/stacked share a builder; line/area share one; pie/donut share one), reads colors from new `--series-1..8`/`--chart-text-*`/`--chart-grid` CSS custom properties in `style.css` (both the dark default and the existing `prefers-color-scheme: light` block), and tracks mounted `Chart` instances in a `Map<heading, Chart>` so `destroyChart`/`destroyAllCharts` can tear them down before their canvas is discarded (every full-grid rebuild from live reload calls `destroyAllCharts()` first; the toggle click handler calls `destroyChart` for one card when switching away from a chart view) — Chart.js doesn't auto-clean instances whose canvas gets detached via `innerHTML =`, so skipping this would leak one `Chart`/`ResizeObserver` per rebuild. `main.ts`'s `viewsFor` now appends one `WidgetView` per `card.chartTypes[]` entry; since Chart.js needs a live canvas (not just an HTML string), `renderCardBody` emits a `<div class="chart-container"><canvas></canvas></div>` placeholder for chart views and a new `mountActiveView()` (called after the body HTML lands in the DOM, both on full rebuild and on toggle click) does the actual `mountChart`/`destroyChart` call. **Bug caught by `table.spec.ts`, not by eye:** a card with a table can now show up to 8 toggle buttons (default, markdown, up to 6 chart types); `.card-toggle` had `flex-shrink: 0` and no wrap, so on an 8-button card the toggle row overflowed the 280px card width and visually spilled into the *next* grid column — Playwright correctly refused to click a button there because the neighboring card's heading intercepted the click point. Fixed with `flex-wrap: wrap` on both `.card-header` and `.card-toggle` (plus `justify-content: flex-end` on the latter so wrapped rows stay right-aligned); confirmed visually after the fix via manual Playwright screenshots (grouped bar, scatter, pie all render correctly with legends/axis titles and no overlap). Added `tests/fixtures/tables.md` (single-series Fruit Counts, two-series Monthly Sales, and a table-less Notes section) and `tests/table.spec.ts` (4 tests: single-series offers Bar/Pie/Donut/Line/Area/Radar but not Grouped/Stacked/Scatter and a canvas mounts on toggle; two-series offers Grouped/Stacked/Scatter but not Bar/Pie; a table-less card offers exactly the 2 baseline toggles; chart choice survives `page.reload()`). Updated `ARCHITECTURE.md` throughout (new `table.ts`/`chart-view.ts` module entries, WS payload shape, build-config exclusions, verification section, the toggle-overflow CSS fix). Full verification green from a clean `dist`/`dist-server`/`test-results`: build, typecheck, `npm test` (11 passed). Noted, not acted on (out of scope, would need a Vite major-version bump incompatible with the Node 18 pin): `npm audit` flags a moderate esbuild/dev-server-only advisory pulled in transitively by Vite 5, unrelated to this increment's `chart.js` addition.
- 2026-07-18 — Increment 8 completed: added `src/parser/tasklist.ts` (`extractTaskItems`), a server-side pass over a card's unmutated token stream that finds every `inline` token whose raw source starts with the same `- [ ]`/`- [x]` marker regex `markCheckboxes` uses, and returns `{ html, done }[]` — `html` is `md.renderInline()` on the text after the marker (preserves bold/links), `done` from the bracket letter. Must run before `markCheckboxes` mutates the shared tokens, same ordering constraint `table.ts`'s `extractTableData` already had; `parse.ts`'s `flush()` now calls both before the mutation and adds `tasks?: TaskItem[]` to `Card` (undefined, not `[]`, when a card has no task items, so it doubles as the "has a task list" check). Added `src/widgets/tasklist-view.ts` (client-only): `taskProgress()` computes `{done, total, percent}`; `taskDonutData()` reshapes that into a two-category `TableData` (`Done`/`Open` counts) so the donut view reuses `chart-view.ts`'s existing `mountChart(..., 'donut')` builder rather than a bespoke chart config — no new Chart.js code needed. `renderTaskItemsHtml()` renders the same per-item checklist (real disabled checkboxes) under both new views, since ELEMENTS.md requires each milestone's individual state to stay visible next to the aggregate, never a bare percentage. `main.ts`'s `WidgetView` gained a `kind` discriminant (replacing the old boolean `isChart`) so `renderCardBody`/`mountActiveView` can branch on `'chart' | 'progress-bar' | 'progress-donut'` in addition to `'default' | 'markdown'`; `viewsFor()` appends the two new views only when `card.tasks?.length`. Added `.progress-bar`/`.progress-bar-fill`/`.progress-label`/`.task-list`/`.task-item`/`.chart-container-small` CSS (light-mode override for the bar's track background). Added `tests/fixtures/tasklist.md` (4-item mixed list, 2 done/2 open, plus a task-less "Notes" card) and `tests/tasklist.spec.ts` (4 tests, port 4324: progress-bar shows "50% complete (2/4)" and all 4 checkbox states correctly; progress-donut mounts a canvas alongside the same checklist; the task-less card offers no progress toggles; choice survives `page.reload()`). Manually screenshot-verified both new views render correctly with no layout overflow (the kind of CSS bug Increment 7 caught late) before committing. Full verification green from a clean `dist`/`dist-server`/`test-results`: build, typecheck, `npm test` (15 passed). Updated `ARCHITECTURE.md` throughout (new `tasklist.ts`/`tasklist-view.ts` module entries, `Card` shape, `main.ts`'s widget-framework paragraph, CSS module note, verification section). Committed `ecd6de3`, pushed to `main`.
- 2026-07-18 — Increment 10 completed: added `mermaid@^11.16.0` (browser-only; `npm install` warned that its transitive `marked` dependency wants Node ≥20 while this machine runs 18.19.1, but `marked`/`mermaid` only ever execute client-side inside the Vite bundle, never under the pinned Node 18 process, so the EBADENGINE warning doesn't apply to this project's actual runtime constraint) and `yaml@^2.9.0` (pure-JS, `engines: node >= 14.6`, no conflict) as dependencies. `src/parser/mermaid.ts`'s `extractMermaidSource` finds a card's first ```` ```mermaid ```` fence (`fence.info`'s first whitespace-separated word, case-insensitive) and returns its raw source; per `ELEMENTS.md` this element has no switchable alternatives, so no shape-validity ladder was needed the way `table.ts`/`chartfence.ts` have one. `src/parser/chartfence.ts`'s `extractChartFence` finds a card's first ```` ```chart ```` fence and parses its body as JSON first, falling back to YAML (JSON is a YAML subset, so trying JSON first avoids YAML's laxer parsing silently accepting a JSON typo) into an invented-this-increment config shape (`{ type?: string, categories: string[], series: { label: string, data: number[] }[] }`, no prior source to follow, same as Increment 7's chart-shape rules and Increment 9's gauge-max rule were invented); reuses `table.ts`'s `validChartTypes` unchanged on the parsed `TableData` (identical shape to a real table's, so no new validity ladder). `type`, if present and valid for the shape, becomes `ChartFence.defaultType`; otherwise the first valid type does. Malformed config (bad JSON/YAML, missing/mismatched-length arrays, non-numeric data) yields `undefined` — same "only alternatives valid for the data shape" fallback-to-plain-render rule `table.ts`'s column rejection already follows. `parse.ts`'s `flush()` calls both extractors (order-independent — neither mutates tokens) and adds `mermaid?: string`/`chartFence?: ChartFence` to `Card`. Client side: `src/widgets/mermaid-view.ts`'s `mountMermaid` calls `mermaid.render()` (initialized once at module load with `theme: 'dark'/'default'` picked from `prefers-color-scheme` — a one-time match, not live-synced to the manual toggle Increment 11 will add, noted below) and injects the resulting SVG, catching parse errors into an inline `.mermaid-error` message instead of letting them crash the dashboard. `main.ts` reuses the existing `card.metric`-style special-casing pattern for both new element types: `card.mermaid` overrides only the `default` view (no new toggle entries — ELEMENTS.md gives mermaid no alternatives) to a `<div class="mermaid-container">` mounted async via `mountMermaid`; `card.chartFence` overrides `default` (unless the card also has a real `card.table`, whose own default wins) to render the fence's `defaultType` as a chart, and appends one `kind: 'chart'` toggle per `chartFence.types` entry with a `fence-`-prefixed id (`fence-pie`, etc.) so it can't collide with a same-named type id from a real table's `card.chartTypes` if a card somehow had both. **Bug caught by `tests/mermaid-chart.spec.ts`, not by eye:** `mermaid.render()` internally stages its output in a hidden `#d<id>` div appended directly to `<body>` and removes it itself once the SVG is extracted — but only on the success path; a parse error skips that cleanup, leaving mermaid's own large error-diagram SVG (bomb icon + "Syntax error in text") floating outside the entire card grid, below the whole dashboard (confirmed via a manual Playwright screenshot before the fix, and again after). Fixed with a `finally` block in `mountMermaid` that removes `#d<id>` unconditionally after every render call. Added `tests/fixtures/diagrams.md` (a valid flowchart, a `bar`-type chart fence with 3 categories/1 series, a deliberately invalid mermaid fence, and a fence-less prose section) and `tests/mermaid-chart.spec.ts` (5 tests, port 4326: flowchart default view is a rendered `<svg>` with no extra toggles beyond default/markdown, and its Markdown view shows the raw fence text; the invalid diagram shows `.mermaid-error` and — the regression check for the bug above — leaves zero `#dmermaid-*` elements on `<body>`; the chart fence defaults to a mounted `bar` canvas and offers `fence-line`/`fence-pie`/`fence-radar` alternatives (not a repeated `fence-bar`), each mounting a canvas and surviving `page.reload()`; a fence-less card offers no diagram/chart toggles). Updated `ARCHITECTURE.md` throughout (new `mermaid.ts`/`chartfence.ts`/`mermaid-view.ts` module entries, `Card` shape, `main.ts`'s widget-framework paragraph, `style.css`'s note, verification section). Full verification green from a clean `dist`/`dist-server`/`test-results`: build (mermaid's diagram-type modules code-split into separate chunks automatically via Vite; the resulting `index` chunk is ~855kB unminified/227kB gzipped — a size *warning* only, build still passes, and trimming it is out of scope for this increment), typecheck, `npm test` (25 passed). Noted for Increment 11 (already added to GOAL.md Status): mermaid's theme is picked once at load, not live-synced to whatever manual light/dark toggle that increment adds.
- 2026-07-18 — Increment 9 completed: added `src/parser/kpi.ts` with two extractors sharing one `Key: value` regex (`^(.+?):\s*([+-]?[\d,]+(?:\.\d+)?)(%)?\s*$`, trailing `%` tracked for later formatting/gauge-max decisions). `extractKpiListItems` scans a card's first bullet/ordered list and requires *every* item to match, or the whole list is rejected (falls back to the plain List default, same "only alternatives valid for the data shape" rule `table.ts` already follows). `extractSingleMetric` only fires when the card has no list and exactly one `inline` token total — a table cell or a second prose paragraph both disqualify it, since ELEMENTS.md calls this the "Single large number" element (singular). `parse.ts`'s `flush()` tries the list shape first, so `Card.kpi`/`Card.metric` are mutually exclusive by construction; added both fields to `Card`. Added `src/widgets/kpi-view.ts` (client-only, mirrors `chart-view.ts`/`tasklist-view.ts`'s "duplicate small types, don't import server code" pattern): `renderKpiTilesHtml`/`renderStatTileHtml` are plain HTML (no canvas); `kpiTableData` reshapes items into `TableData` so Bar/Pie reuse `chart-view.ts`'s existing `mountChart` builder, same reuse pattern as the Increment 8 progress-donut. The Gauge view has no Chart.js equivalent, so it's a `doughnut` hack: `circumference: 180`/`rotation: 270`/`cutout: '75%'`, a two-segment `[value, max - value]` dataset, and a small inline `afterDraw` plugin drawing the formatted value in the arc's center (Chart.js has no center-text support built in). Gauge `max` isn't in the source Markdown, so it's inferred (decided this increment, no prior source): a `%` value maxes at 100; otherwise the next power of ten strictly above the value (minimum 10) — documented inline and in ARCHITECTURE.md since it's an invented rule like Increment 7's chart-shape-validity ladder was. Gauges are tracked in their own `Map<heading, Chart>` (`destroyGauge`/`destroyAllGauges`), separate from `chart-view.ts`'s chart map; `main.ts`'s `mountActiveView` now clears *both* maps unconditionally before mounting, since a card's canvas is always replaced on toggle regardless of which map it was tracked in — skipping this would leak a `Chart` instance any time a card switches from a regular/task chart to a gauge or vice versa. `main.ts` also special-cases `viewId === 'default' && card.metric` to render `renderStatTileHtml` instead of `card.html` — ELEMENTS.md's default widget for a lone metric line is already a stat tile, the first element (after Table) where "default" isn't the plain markdown-it render. Added `tests/fixtures/kpi.md` (a 3-item numeric list, a lone `Metric: 99.9%` line, and a number-less "Notes" card) and `tests/kpi.spec.ts` (5 tests, port 4325: numeric list defaults to plain list text and offers KPI-tiles/Bar/Pie with correct tile contents; Bar/Pie each mount a canvas; the lone metric defaults to a stat tile showing "99.9%" and offers a Gauge that mounts a canvas; the number-less card offers no KPI/Gauge toggles; KPI view choice survives `page.reload()`). Manually screenshot-verified the KPI tiles and gauge render cleanly with no overflow (same check Increments 7-8 did) — gauge's tiny gray remainder segment for 99.9/100 is barely visible, which is correct given the data, not a bug. Full verification green from a clean `dist`/`dist-server`/`test-results`: build, typecheck, `npm test` (20 passed). Updated `ARCHITECTURE.md` throughout (new `kpi.ts`/`kpi-view.ts` module entries, `Card` shape, `main.ts`'s widget-framework paragraph, Build section's browser-only-types note, verification section). Committed `ddd6eed`, pushed to `main`.
- 2026-07-18 — Increment 12 completed: rewrote `README.md` from the two-paragraph scaffold-era stub into full usage docs — a Features list, a "Quick start" that clones/builds/runs against the new demo fixture (explicitly notes the package is `private: true` and not published to npm, so `npx md-dashboard` was deliberately *not* claimed — checked `package.json` before writing the install instructions rather than assuming), a "How it works" section pointing at `ARCHITECTURE.md`/`ELEMENTS.md`, and a trimmed Development section (removed a redundant "run the built CLI" snippet that duplicated Quick start). Verified the `--port` default (`4173`, from `src/cli.ts`'s `DEFAULT_PORT`) by reading the source rather than guessing. Added `examples/demo.md`, a new demo fixture (separate from the `tests/fixtures/*.md` files, which are minimal and test-shaped, not representative) — a fictional "Project Nova" sprint dashboard using every widget type together in one realistic-looking document: prose, a task checklist, two tables (single- and multi-series, so both chart-alternative ladders are reachable), a numeric KPI list, a single metric, a mermaid flowchart, a `chart` fence, a blockquote, a code block, and an image (a stable `picsum.photos` seeded URL so the screenshot is reproducible). Built the project, ran the compiled CLI against the demo fixture on a scratch port, and used a throwaway Playwright script (run from inside the repo so `@playwright/test` resolves — running it from `/tmp` failed with `ERR_MODULE_NOT_FOUND` first try) to capture three real screenshots into `docs/screenshots/`: dark mode, light mode (via `colorScheme` emulation), and one card toggled to a Grouped Bar chart (had to select the toggle button by its `title` attribute, not an accessible-name role query — the buttons' visible/accessible text is an emoji icon, and the descriptive label lives only in `title`, not exposed as the a11y name). Viewed all three screenshots before embedding them to confirm they actually show the intended state (no blank canvases, correct theme, chart genuinely toggled) rather than trusting the script ran correctly. Cleaned up the scratch server process and the temporary screenshot script afterward; `docs/` and `examples/` are not covered by `.gitignore`, confirmed via `git status` before staging. Full verification green from a clean `dist`/`dist-server`/`test-results`: build, typecheck, `npm test` (31 passed, unchanged — this increment touched no application code). Committed `aecc14e`, pushed to `main`.
- 2026-07-18 04:17 — Budget re-check before starting Increment 13: session at 85% (hits the pause threshold), resets 2026-07-18 06:49 Europe/Berlin; week healthy at 40%. No work done this run — armed next run for 2026-07-18 06:51 (reset + 2min buffer) via `.hustle/bin/hustle-scheduler.sh arm --at`. Next run should proceed straight to Increment 13.
- 2026-07-18 — Increment 11 completed: theming switched from `@media (prefers-color-scheme: light)` to an attribute-driven system (`:root[data-theme='dark' | 'light']` in `src/style.css`, dark is the base `:root` block, light is the override) so a manual choice can beat the system preference — CSS alone can't express "override the media query," so `data-theme` has to be set and kept in sync by JS. `index.html` gained a small inline `<script>` in `<head>` that resolves the initial theme (stored `localStorage['md-dashboard:theme']` override, else `matchMedia('(prefers-color-scheme: dark)')`) and sets `data-theme` synchronously before first paint, avoiding a flash of the wrong theme; `src/main.ts`'s new theme module duplicates that same resolution (can't share code across an inline HTML script and a bundled TS module) and then owns everything after boot: a `#theme-toggle` button (🌙/☀️, added next to `<h1>` in a new `.dashboard-header` flex row) flips the theme and stores the override; a `matchMedia` `change` listener keeps following the system preference for as long as no override is stored. Cleared the noted blocker from Increment 10: `mermaid-view.ts`'s one-time `mermaid.initialize` became an exported `setMermaidTheme(theme)`, called at every theme flip; since neither Chart.js (colors read from CSS custom properties at mount time, not live-bound) nor mermaid (static SVG, no live theme switch) update on their own, `applyTheme` re-renders the last-received WebSocket message (new `lastMessage` cache) on every flip so mounted charts/diagrams redraw with the new palette — confirmed visually via manual Playwright screenshots toggling a page with both a flowchart and a chart mounted (see this session's transcript: dark→light, diagram box fills/text and chart axis/grid colors all switch correctly, zero orphaned mermaid staging `<div>`s, exactly one mounted `<svg>`, no leaked `Chart` instance). Added explicit responsive breakpoints to `.dashboard-grid` (`max-width: 640px` → single column, tighter padding, smaller `h1`; `641–1024px` → narrower `minmax` floor so 2+ columns keep fitting; `>1024px` → the pre-existing `auto-fill, minmax(280px, 1fr)`), confirmed visually at a 375px mobile viewport (single-column stack) via screenshot. Robustness: `storedViewId` (now takes the whole `Card`, not just its heading) clamps a persisted view id to one still valid for that card's *current* `viewsFor(card)` shape, falling back to `'default'` — otherwise a stored id gone stale (e.g. a table lost a column and no longer offers Scatter after a live-reload edit) would leave no toggle button showing as pressed. Error handling: `server.ts`'s `parseDocument` call is now wrapped in a `safeParse` try/catch (the target file is user input edited outside this process — a system boundary — so a parse failure or a read mid-save must never crash the server/watcher; on failure it logs and skips sending, leaving whatever was last broadcast in place) — markdown-it itself is lenient enough that this is defense-in-depth rather than a reproducible crash path found this increment. A well-formed but section-less document (no `##` cards — confirmed empty/preamble-only files parse fine, this isn't a parse error) now renders a `.empty-state` message client-side instead of a silently blank grid. Added `tests/fixtures/empty.md` (title, no `##` sections) and `tests/empty-doc.spec.ts` (port 4328: empty state visible, zero `.card` elements, title still shown) plus `tests/responsive-theme.spec.ts` (port 4327, reusing `tests/fixtures/elements.md`'s 3 cards: theme defaults to `prefers-color-scheme` in both directions via `page.emulateMedia`; a manual toggle click overrides the system preference and the override survives a reload even after the system preference flips back, with the `localStorage` value asserted directly; a 375px viewport stacks all 3 cards into a single column, a 1280px viewport lays the first two side by side, both asserted via `boundingBox()` comparisons rather than reading `grid-template-columns` directly). Updated `ARCHITECTURE.md` throughout (`main.ts`'s theme module, `mermaid-view.ts`'s `setMermaidTheme`, `chart-view.ts`'s now-attribute-driven color note, `style.css`'s theming/breakpoints paragraph, `server.ts`'s `safeParse`/empty-cards note, verification section). Full verification green from a clean `dist`/`dist-server`/`test-results`: build, typecheck, `npm test` (31 passed, up from 25).
- 2026-07-18 06:51 — Budget re-check before Increment 13: session reset to 0% (the armed run landed right at the 06:51 reset), week healthy at 41%. Proceeded straight to Increment 13.
- 2026-07-18 — Increment 13 completed: `md-dashboard <target>` now accepts a folder as well as a file. New `src/server/discover.ts`'s `discoverMarkdownFiles` recursively walks a directory (skipping dotfiles/dirs and `node_modules`) and returns every `.md` file as `{ id, absPath }` (`id` = forward-slashed path relative to the folder, sorted); `src/cli.ts` calls it when the target is a directory (erroring out if it contains zero `.md` files) or wraps a single file as a one-entry list — single-file mode is now just folder mode with one file, not a separate code path. `src/server/watch.ts`'s `watchFile` was generalized to `watchFiles` (chokidar accepts an array of paths natively, so this was a rename + one extra parameter, not new logic) — the callback now receives which absolute path changed. `src/server/server.ts` sends a `{ type: 'files', files: string[] }` message on every new WS connection (every discovered file's id) followed by one `{ type: 'content', file, title, cards }` per file; on a watched file's change, only that file's fresh `content` is broadcast — not a full resend of every file — since the client keeps its own per-file cache. Client-side, `src/main.ts` added a `documents: Map<string, ContentMessage>` cache, `knownFiles`/`selectedFile` state (selection persisted in `localStorage['md-dashboard:selectedFile']`, clamped to a still-valid file the same way `storedViewId` already clamped stale view ids), and a `#file-nav` sidebar rendered into a new `.dashboard-body` flex wrapper (sidebar + `.dashboard-main`) — `renderFileNav` sets `hidden` on the nav whenever `knownFiles.length <= 1`, so single-file mode's DOM is byte-for-byte unchanged from before this increment (verified both by a screenshot and by a dedicated regression test). Clicking a sidebar entry calls `selectFile`, which re-renders from the cached `documents` entry with **no server round-trip** — every file's content was already pushed right after the `files` message at connect. Added CSS for `.dashboard-body`/`.file-nav`/`.file-nav-btn`/`.dashboard-main` (light/dark variants) plus a `max-width: 640px` rule that collapses the sidebar into a wrapped button row above the content, same breakpoint the card grid already collapses at. Added `tests/fixtures/folder/{alpha,beta}.md` and `tests/fixtures/folder/nested/gamma.md` (to prove recursive discovery, not just top-level) and `tests/folder.spec.ts`: one `describe` block (serial, one spawned CLI on port 4329) verifies the sidebar lists all three files including the nested one's full relative id, `alpha.md` (first alphabetically) renders by default, clicking `beta.md`/`nested/gamma.md` switches the rendered dashboard, and the selection survives a full page reload; a second `describe` block re-spawns the CLI against `tests/fixtures/sample.md` (port 4330) to confirm `#file-nav` stays `hidden` in single-file mode — the regression check for "folder mode must not change single-file UX". Manually verified live-reload targeting (a raw WebSocket script confirmed editing `beta.md` only pushes a `content` message for `file: "beta.md"`, not the other two files) and the CLI's error paths (empty directory, nonexistent path) before committing. Full verification green from a clean `dist`/`dist-server`/`test-results`: build, typecheck, `npm test` (36 passed, up from 31). Updated `ARCHITECTURE.md` throughout (new `discover.ts` entry, `cli.ts`/`server.ts`/`watch.ts`/`main.ts`/`style.css` sections, live-reload flow, verification test list) and `README.md` (folder-mode usage, Features, How it works, Status). Committed `0452689`, pushed to `main`. Deliberate scope limit, noted rather than silently assumed: files added to a watched folder after CLI startup are not picked up — the file set is fixed at discovery time; re-running the CLI is the workaround, and nothing in the North Star/DoD requires live add/remove. Next up: the Closeout final sweep — every DoD checkbox should now be satisfiable; go through them one by one against the current app rather than assuming.
- 2026-07-18 07:12 — Final sweep completed, goal COMPLETE. Verified from a clean `dist`/`dist-server`/`test-results`: `npm run build`, `npm run typecheck`, `npm test` all green (36 passed). Confirmed `git remote -v`/`git log origin/main` match local `main` (`940cd32`) — repo is pushed to the public GitHub remote. Rather than trust the increment logs, drove the actual running app: built the CLI, served `examples/demo.md` on a scratch port, and used a throwaway Playwright script (run from inside the repo per the known `@playwright/test` resolution gotcha, deleted afterward) to screenshot and interact with it directly — confirmed all 10 demo cards render (prose, task checklist, two tables, KPI list, single metric, mermaid flowchart, chart fence), the theme toggle flips light↔dark and the override survives a full page reload (`localStorage['md-dashboard:theme']` read back directly), and toggling the Team Velocity table to a Bar chart mounts a real canvas. Checked every DoD checkbox in this file to `[x]` — each one was independently verified this iteration (not just inferred from having reached the last increment): responsive grid, per-ELEMENTS.md default widgets, toggle+Markdown-raw-render+localStorage persistence, live reload (already covered by `tests/watch.spec.ts`, not re-run manually this iteration), light/dark with persisted override, and the full green build/typecheck/test pipeline plus the pushed public remote. No code changes this iteration — closeout was verification-only, everything was already at spec from Increments 1-13. STATUS set to COMPLETE; no next action, no wakeup armed.
- 2026-07-18 — v2 chart-first pivot, implemented interactively (not by the loop) after user feedback that the dashboard read as "text + chart" rather than chart-first, and that checklist charts hid the individual milestones. Rewrote `ELEMENTS.md` as the v2 contract, then the code: `chooseDefaultChart` in `parser/table.ts` picks a table's default from its data shape (Line for a time-like first column — all categories must match a year/ISO-date/quarter/month pattern, so a "Total" row can't fake a trend; Grouped Bar for 2+ series; Bar otherwise), computed server-side and shipped as `Card.defaultChartType` so the client needn't pull the parser in. Tables/task lists/numeric lists now default to their chart and the plain table/checklist/list became toggle *alternatives* (new `table`/`checklist`/`kpi-list` views). `tasklist-view.ts` was rewritten: the old two-slice `['Done','Open']` donut is gone — every item is now its own equal-sized segment, coloured by status, with a shared tooltip on hover *and* tap (touch has no hover, so `pointerover` + an explicit `click` handler, dismissed by an outside `pointerdown`); `mountTaskChart` in `chart-view.ts` does the same for pie/donut and deliberately bypasses `foldToOther`, since colour encodes a two-state status rather than a category and folding to 8 slots would destroy the per-item detail the view exists for. Above 20 items a `<details>` disclosure adds a readable item list, as segments get too thin to target. Text became secondary but never lost: new top-level-paragraph extraction (`level === 0` only, so list/blockquote/table text isn't duplicated, and a lone `Metric:` card yields none) renders as a caption under the chart, clamped to 2 lines with an inline Read more that `wireReadMore` only reveals for text that actually overflows; prose-only sections still get a de-emphasised `.card-prose` card with the same treatment. Added a global `#markdown-toggle` showing the whole original document at once, alongside the existing per-card Markdown mode. Two fixes found only by looking at the running app rather than the test output: `.dashboard-grid` needed `align-items: start` (chart-first widgets are far more compact than the tables they replaced, so stretched rows left cards mostly empty), and the segment tooltip had to move below the bar with `width: max-content` plus edge clamping (above it covered the card heading, and shrink-to-fit crushed it into a narrow column near the right edge). Tests: updated the four specs that asserted the old table-first/list-first defaults and the donut-plus-checklist behaviour — they broke by design, not by accident — and added `tests/chart-first.spec.ts` (+ fixture) for caption clamping, Read more expansion, prose-card demotion, short text getting no pointless control, and the global document view; `tasklist.spec.ts` gained per-item segment and hover/tap tooltip coverage, `table.spec.ts` a `data-default-chart` assertion proving Fruit Counts picks Bar and Monthly Sales picks Line. Verified green: build, typecheck, `npm test` (43 passed, up from 36), plus screenshots of both themes, the tooltip at both bar edges, and the global document view.
